// controllers/horariosController.js
const pool = require('../config/db'); // O donde tengas tu conexión a PostgreSQL

const guardarHorarios = async (req, res) => {
  // ... Todo el código que me pasaste ...
};

module.exports = {
  guardarHorarios,
};